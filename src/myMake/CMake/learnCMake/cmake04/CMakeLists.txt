PROJECT (HELLO) #工程名，工程名和生成的可执行文件hello是没有任务关系的
add_subdirectory(lib bin)   #向当前⼯程添加存放源⽂件的⼦⽬录lib，并可以指定中间⼆进制和⽬标⼆进制存放的位置，bin文件夹会被自动创建
                            #如果不进⾏ bin ⽬录的指定，那么编译结果(包括中间结果)都将存放在build/bin ⽬录

#注意：一般可以不在以上命令中指定bin的路径，避免浪费内存